export class TarefasNC {
    titulo: string;
    descricao: string;
    data: string;
  
  
    constructor(titulo: string, descricao: string, data: string) {
      this.titulo = titulo;
      this.descricao = descricao;
      this.data = data;
    };
};

export class TarefasC {
    titulo: string;
    descricao: string;
    data: string;
  
  
    constructor(titulo: string, descricao: string, data: string) {
      this.titulo = titulo;
      this.descricao = descricao;
      this.data = data;
    };
};